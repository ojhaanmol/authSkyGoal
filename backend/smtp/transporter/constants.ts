import configurations from "../../configurations"

export const SMTP_HOST = configurations.SMTP_HOST;
export const SMTP_PORT = configurations.SMTP_PORT;
export const SMTP_AUTH_USER = configurations.SMTP_AUTH_USER;
export const SMTP_AUTH_PASSWORD = configurations.SMTP_AUTH_PASSWORD;